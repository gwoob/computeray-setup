post_pkg_postinst() {

# symlink package into steam directory
ln -s /opt/proton-ge-custom/* /usr/share/steam/compatibilitytools.d

# remove old package symlinks
find /usr/share/steam/compatibilitytools.d -type l ! -exec test -e {} \; -delete

}
